<resources>

    <!-- Base application theme (Material 3 / DayNight). -->
    <!-- ActionBar は Compose 側で TopAppBar を使うため NoActionBar を採用 -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar" />

</resources>
